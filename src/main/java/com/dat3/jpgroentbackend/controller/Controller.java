package com.dat3.jpgroentbackend.controller;


import org.springframework.web.bind.annotation.*;

//@ApiResponse(content = { @Content(mediaType = "application/json"));
@RestController
public class Controller {
    /*
    @GetMapping("/hello/{language}")
    public SimpleMessage hello(
        @PathVariable("language") String language
    ) {
        return new SimpleMessage("Hello world! Sorry i don't speak " + language);
    }
    @PostMapping("/hello")
    public SimpleMessage postme() {
        return new SimpleMessage("Hello world!");
    }
    */
}
